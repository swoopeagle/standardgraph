
[project]
name = "singapore-moe"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["shared"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.uv.sources]
shared = { workspace = true }
